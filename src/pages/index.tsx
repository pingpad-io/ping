import Feed from "~/components/Feed";
import { PageLayout } from "~/components/Layout";

const HomePage = () => {
  return (
    <PageLayout>
      <Feed />
    </PageLayout>
  );
};

export default HomePage;
